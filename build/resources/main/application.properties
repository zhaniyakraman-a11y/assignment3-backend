spring.application.name=project

# Database - можно переопределить через переменные окружения
spring.datasource.url=jdbc:postgresql://localhost:5432/assignment3
spring.datasource.username=postgres
spring.datasource.password=askme2006
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}

# JPA - можно переопределить через переменные окружения
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL:true}
spring.jpa.properties.hibernate.dialect=${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQLDialect}

# JWT
security.jwt.secret-key=${JWT_SECRET_KEY:8f1e2d3c4b5a6978a9b8c7d6e5f4a3b2}
security.jwt.expiration-time=86400000
security.jwt.algorithm=HS256

# Application
app.images.base-dir=${APP_IMAGES_BASE_DIR:images}
api.version=${API_VERSION:/api/v1}

# Server - поддержка PORT от Render.com
server.port=${PORT:${SERVER_PORT:8081}}
server.address=0.0.0.0